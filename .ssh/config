Include config_private
Include ~/.colima/ssh_config
Include ~/bakdata/.ssh_config

Host *
    Compression yes
    SendEnv COLORTERM
    IdentityAgent ~/.1password/agent.sock

Host github.com
  Hostname ssh.github.com
  Port 443 # default port 22 may be blocked
